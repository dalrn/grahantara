# data/

Semua isi folder ini di-.gitignore. Jangan commit data mentah.

- `raw/`       Data apa adanya dari sumber. Tidak pernah diedit.
- `interim/`   Hasil antara (Parquet).
- `processed/` Siap dipakai. Output akhir disalin ke `web/public/data/`.

Simpan `Hasil_Survei_BERSIH.xlsx` di `raw/`.
