# ---------------------------------------------------------------------------
# Kunci untuk PIPELINE saja (dijalankan lokal, tidak pernah di-deploy).
# Jangan menambahkan kunci ini ke Vercel — tidak ada kode di web/ yang membacanya.
# ---------------------------------------------------------------------------
MAPID_API_KEY_MISSION=
MAPID_API_KEY_DATA=
MAPID_PROJECT_ID=

# Google Earth Engine project id (W2 NDVI)
EE_PROJECT=

# ---------------------------------------------------------------------------
# Kunci untuk FRONTEND ada di web/.env.example, BUKAN di sini.
# Yang dipakai hanya dua:
#   VITE_MAPID_BASEMAP_KEY   basemap, boleh sampai ke browser
#   DEEPSEEK_API_KEY         AI-1/2/4, HANYA sisi server, jangan diberi awalan VITE_
# ---------------------------------------------------------------------------
